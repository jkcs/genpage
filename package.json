{
  "name": "genpage",
  "private": true,
  "scripts": {
    "serve": "lerna run --stream --scope @genpage/gen-platform serve",
    "build:ailment": "lerna run --stream --scope @genpage/aliment build:lib"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": ["packages/*"]
}
